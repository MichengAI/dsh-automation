# I001 任务自动化插件

- 状态：进行中
- 负责人：维护者
- 优先级：高
- 更新时间：2026-09-02 23:50（Asia/Shanghai）

## 目标与业务边界

实现 DeepSeek Harness 的独立任务自动化：用户或 Agent 保存一条自包含任务，Host 到期后在全新 Session 中执行，并留下可审计历史。设置页提供任务列表、执行记录和新建模态。

## 任务清单

- [x] 领域模型、周期计算
- [x] Service / Executor / Tools / RPC
- [x] Web Automations 页
- [x] 原生侧栏定时页签：包裹官方任务树，不依赖 dsh-codex-ui
- [x] 单测与包契约
- [x] 定时侧栏会话菜单互斥，避免点第二条时与第一条重叠
- [x] 新建弹窗只允许选择 Host 已注册工作区，新增目录统一从 Host 工作区入口完成
- [x] 修复任务树过滤导致的 Loading plugins / 点任务卡死
- [x] 会话创建定时任务按官方审批策略分流，并补系统提示避免走 crontab
- [x] 工作区标题栏对齐官方位置与搜索点击
- [x] DSH 稳定版 `>=0.1.1-rc.2 <0.2.0` 兼容及 alpha.5 显式支持与验证基线迁移，设置标题增加 GitHub 与 Issues 入口
- [ ] 真实 Web Profile 组合验收

## 阻塞事项

无；alpha.5 预发布版本组合验收待在真实 DSH Web Profile 执行。

## 验收标准

1. Web 和 Agent 都能创建、列出、暂停、恢复、立即运行和删除。
2. 到期后创建新 Session，消息 source 为 `automation`。
4. 重启不会偷偷重跑遗留 running。
5. 删除定义不删除运行历史。

## 已完成内容

仓库内已落地 Host 插件、Web 客户端、原生侧栏定时页签、文档和单测。定时会话更多菜单改为列表级互斥，并用固定定位弹出，避免两条菜单叠在一起。新建弹窗只消费 Host 已注册工作区，避免插件 RPC 自行扩大目录边界；点遮罩不再关闭。DSH 稳定版 peer 兼容范围为 `>=0.1.1-rc.2 <0.2.0`，并显式支持至 `0.1.2-alpha.5`，开发与最高验证基线为该 alpha 版本；客户端仅声明旧新版本共有的注入，并在设置标题旁加入 GitHub 与 Issues 入口。

## 待处理事项

真实 DSH `0.1.2-alpha.5` Host 上重启 Web，验证设置页、侧栏、创建和立即运行。

## 关联提交

待发布 `@michengai/dsh-automation@0.1.23`。

## 阅读顺序

1. [项目概览](..\..\00-交接入口\01-项目概览.md)
2. [调度与执行模型](..\..\03-技术架构\01-调度与执行模型.md)



